{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./dist && microbundle build --entry ./client.js --output ./dist/fragments.js --format iife --target web --external none --globals window",
    "build2": "rm -rf ./dist && rollup ./client.js --file ./dist/fragments.js --format iife",
    "dev": "node ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "microbundle": "^0.14.2",
    "rollup": "^2.67.3"
  }
}
